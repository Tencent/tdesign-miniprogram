<view class="demo-rate">
  <view class="demo-rate__title">规格 20</view>
  <t-rate value="{{value[0]}}" data-index="{{0}}" variant="filled" bind:change="onChange" />
</view>
<view class="demo-rate">
  <view class="demo-rate__title">规格 16</view>
  <t-rate value="{{value[1]}}" data-index="{{1}}" variant="filled" bind:change="onChange" size="{{16}}" />
</view>
