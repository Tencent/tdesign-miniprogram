<wxs src="./badge.wxs" module="this"></wxs>

<view class="{{this.getBadgeOuterClass({shape})}}">
  <view
    class="{{this.getBadgeInnerClass({dot, size, shape, hasSlot})}}"
    style="{{this.getBadgeStyles({color, offset})}}"
    >{{ this.getBadgeValue({dot, content, count, maxCount}) }}</view
  >
  <slot />
</view>
