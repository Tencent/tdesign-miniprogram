<t-button variant="outline" bind:tap="openDrawerBase" block size="large" theme="primary">With Footer</t-button>

<t-drawer
  visible="{{visible}}"
  placement="{{placement}}"
  title="Title"
  items="{{sidebar}}"
  bind:overlay-click="overlayClick"
  bind:item-click="itemClick"
>
  <view class="button-host">
    <t-button slot="footer" size="large=" block variant="outline">Action</t-button>
  </view>
</t-drawer>
