<t-button variant="outline" bind:tap="openDrawerBase" block size="large" theme="primary">With Title</t-button>

<t-drawer
  visible="{{visible}}"
  placement="{{placement}}"
  title="Title"
  items="{{sidebar}}"
  bind:overlay-click="overlayClick"
  bind:item-click="itemClick"
></t-drawer>
