<t-calendar
  visible="{{visible}}"
  title="Calendar"
  value="{{value}}"
  confirmBtn="{{confirmBtn}}"
  minDate="{{minDate}}"
  maxDate="{{maxDate}}"
  format="{{format}}"
  bind:confirm="handleConfirm"
/>
<t-cell arrow title="Custom Date Text" bind:tap="handleCalendar" />
