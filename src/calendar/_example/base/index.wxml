<t-calendar
  visible="{{visible}}"
  title="Calendar"
  confirmBtn="{{confirmBtn}}"
  bind:confirm="handleConfirm"
  bind:close="onClose"
/>
<t-cell arrow title="Select Single Date" bind:tap="handleCalendar" note="{{note}}" />
