<t-tab-bar defaultValue="label_1" theme="tag" split="{{false}}">
  <t-tab-bar-item wx:for="{{list}}" wx:key="index" value="{{item.value}}" sub-tab-bar="{{item.children}}">
    {{item.label}}
  </t-tab-bar-item>
</t-tab-bar>
