.t-qrcode-container {
  display: flex;
  flex-direction: column;
  align-items: start;
  gap: 20rpx;
}

.intro-text {
  font-size: 14px;
  line-height: 22px;
  font-weight: 400;
  color: rgba(0, 0, 0, 0.6);
}
