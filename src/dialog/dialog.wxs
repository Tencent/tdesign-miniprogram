module.exports.getTypeof = function (obj) {
  return typeof obj;
};

module.exports.getBtnClass = function (prefix, buttonLayout) {
  var cls = [prefix + '__button'];

  if (buttonLayout) {
    cls.push(prefix + '__button--' + buttonLayout);
  }
  return cls.join(' ');
};
