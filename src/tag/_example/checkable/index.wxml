<t-check-tag
  wx:for="{{items}}"
  wx:for-index="index"
  wx:key="index"
  checked="{{item.checked}}"
  size="large"
  variant="{{item.variant}}"
  data-index="{{index}}"
  bind:change="handleCheckTagChange"
  >标签文字</t-check-tag
>
