<view class="wrapper">
  <block wx:for="{{items}}" wx:key="index">
    <view class="block">
      <text>{{item}}</text>
      <t-check-tag size="large" variant="{{item}}">未选中态</t-check-tag>
      <t-check-tag checked size="large" variant="{{item}}">已选中态</t-check-tag>
    </view>
  </block>
</view>
