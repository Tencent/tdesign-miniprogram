<view class="{{classPrefix}}">
  <scroll-view
    class="{{classPrefix}}__content"
    scroll-y
    scroll-into-view="{{ currentGroup.index }}"
    scroll-with-animation="{{ false }}"
    style="height: {{ height ? height + 'px' : clientHeight + 'px' }};"
  >
    <t-cell-group
      t-class="{{classPrefix}}__group"
      wx:for="{{ groups }}"
      wx:key="index"
      wx:for-item="group"
      wx:for-index="gindex"
      id="{{ group.index }}"
      title="{{ group.title }}"
    >
      <t-cell
        t-class="{{classPrefix}}__row"
        wx:for="{{ group.children }}"
        wx:key="title"
        wx:for-item="row"
        wx:for-index="rindex"
        bind:tap="onCellTap"
        data-indexes="{{ [gindex, rindex] }}"
        title="{{ row.title }}"
      ></t-cell>
    </t-cell-group>
  </scroll-view>
  <view class="{{classPrefix}}__index">
    <t-icon name="location" t-class="index-icon" size=""></t-icon>
    <view
      class="{{classPrefix}}__btn-bar"
      id="{{classPrefix}}__btn-bar"
      catch:touchstart="onTouchStart"
      catch:touchmove="onTouchMove"
      catch:touchcancel="onTouchCancel"
      catch:touchend="onTouchEnd"
    >
      <view
        class="{{classPrefix}}__btn {{currentGroup.index === group.index ? 'is-actived' : ''}}"
        wx:for="{{ groups }}"
        wx:key="index"
        wx:for-item="group"
        data-group="{{ group }}"
      >
        {{ group.index }}
        <view
          class="{{classPrefix}}__tips"
          wx:if="{{ showScrollTip && currentGroup.index === group.index }}"
        >
          <view class="{{classPrefix}}__tips-text">{{ currentGroup.index }}</view>
        </view>
      </view>
    </view>
  </view>
</view>
