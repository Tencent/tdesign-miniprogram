<wxs src="../common/utils.wxs" module="utils" />

<view wx:if="{{ visible }}" class="{{ rootClasses }}" style="z-index: {{ zIndex }};">
  <!-- icon暂未实现，先等t-icon完成 -->
  <span class="{{ messageClasses }}">{{ content }}</span>
  <slot></slot>
</view>
