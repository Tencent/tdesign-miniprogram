<view class="demo">
  <view class="demo-title">BackTop</view>
  <view class="demo-desc">
    When page is to long to slide down, Backtop is button used to help user quickly return to the top of the page.
  </view>

  <t-demo title="Component Type" desc="Round of BackTop" padding>
    <t-button theme="primary" size="large" block variant="outline" data-source="round" bindtap="onBtnClick">
      Round of BackTop
    </t-button>
  </t-demo>
  <t-demo desc="Half-round of BackTop" padding>
    <t-button theme="primary" size="large" block variant="outline" data-source="half-round" bindtap="onBtnClick">
      Half-round of BackTop
    </t-button>
  </t-demo>
  <t-demo padding>
    <view class="container-flex">
      <t-skeleton rowCol="{{rowCol}}" class="skeleton-item" loading></t-skeleton>
      <t-skeleton rowCol="{{rowCol}}" class="skeleton-item" loading></t-skeleton>
      <t-skeleton rowCol="{{rowCol}}" class="skeleton-item" loading></t-skeleton>
      <t-skeleton rowCol="{{rowCol}}" class="skeleton-item" loading></t-skeleton>
      <t-skeleton rowCol="{{rowCol}}" class="skeleton-item" loading></t-skeleton>
      <t-skeleton rowCol="{{rowCol}}" class="skeleton-item" loading></t-skeleton>
      <t-skeleton rowCol="{{rowCol}}" class="skeleton-item" loading></t-skeleton>
      <t-skeleton rowCol="{{rowCol}}" class="skeleton-item" loading></t-skeleton>
    </view>
  </t-demo>

  <base wx:if="{{type == 'round'}}" scroll-top="{{scrollTop}}" />
  <half-round wx:if="{{type == 'half-round'}}" scroll-top="{{scrollTop}}" />
</view>
