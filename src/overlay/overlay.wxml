<view
  wx:if="{{visible}}"
  class="{{prefix}}-overlay {{prefix}}-overlay--{{visible ? 'active' : ''}}"
  style="z-index: {{zIndex}}; transition: background-color {{duration}}ms ease;"
  bind:tap="handleClick"
>
  <slot />
</view>
