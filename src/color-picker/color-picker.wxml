<wxs src="../common/utils.wxs" module="_" />

<t-popup
  wx:if="{{usePopup}}"
  class="class"
  visible="{{visible}}"
  usingCustomNavbar="{{popupProps.usingCustomNavbar || false}}"
  placement="bottom"
  bind:visible-change="onVisibleChange"
>
  <include src="./template.wxml" />
</t-popup>
<block wx:else>
  <include src="./template.wxml" />
</block>
