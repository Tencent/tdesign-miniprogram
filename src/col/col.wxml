<wxs src="../common/utils.wxs" module="_" />
<wxs src="./col.wxs" module="utils" />

<view
  class="{{prefix}}-class {{ _.cls(classPrefix, [_.formatNumToString(span)]) }} {{ offset ? classPrefix + '--offset-' + offset : '' }}"
  style="{{ utils.getColStyles({ gutter }) }}"
>
  <slot />
</view>
