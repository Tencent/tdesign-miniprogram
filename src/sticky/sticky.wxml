<view class="{{classPrefix}} {{prefix}}-class" style="z-index:{{ zIndex }};{{ containerStyle }}">
  <view class="{{prefix}}-sticky__content" style="z-index:{{ zIndex }};{{ contentStyle }}">
    <slot />
  </view>
</view>
