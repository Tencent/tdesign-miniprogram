<view class="{{classPrefix}}-container" style="z-index:{{ zIndex }};{{ containerStyle }}">
  <view class="{{classPrefix}}__content" style="z-index:{{ zIndex }};{{ contentStyle }}">
    <slot />
  </view>
</view>
