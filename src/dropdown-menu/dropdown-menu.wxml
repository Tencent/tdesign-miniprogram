<view class="{{prefix}}">
  <view class="{{prefix}}__bar">
    <view wx:for="{{titles}}" wx:key="item" class="{{prefix}}__item">
      <view class="{{prefix}}__title">{{item}}</view>
    </view>
  </view>
  <slot></slot>
</view>
