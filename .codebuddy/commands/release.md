---
description: "📦 智能发版 - 自动更新版本号和changelog，执行构建验证并创建发版MR"
argument-hint: "[custom-prompt]"
---

# 📦 智能发版流程

自动化发版流程：分析变更 → 验证构建 → 更新版本 → 创建分支 → 合并 Changesets → 执行测试 → 生成 Release Notes → 提交推送 → 企业微信机器人通知 → IDE 中内置浏览器进行预览 MR 地址

## 📋 执行流程

| 步骤 | 操作 | 说明 | 必须 |
|-----|------|------|-----|
| 1️⃣ | **Git 变更分析** | 使用 `git-analyzer` skill 分析变更范围 | ✅ |
| 2️⃣ | **构建验证** | 运行 `npm run build:uni-vue2` 确保代码质量 | ✅ |
| 3️⃣ | **版本号管理** | 使用 `version-manager` skill 更新版本号 | ✅ |
| 4️⃣ | **发版分支管理** | 使用 `release-branch-manager` skill 创建发版分支 | - |
| 5️⃣ | **合并 Changesets** | 使用 `release-changelog-updater` skill 更新 CHANGELOG | ✅ |
| 6️⃣ |**执行测试** | 使用 `release-changelog-updater` skill 更新 CHANGELOG | ✅ |
| 7️⃣ | **Release Notes 生成** | 使用 `release-notes-generator` skill 生成用户文档 | ✅ |
| 8️⃣ | **提交推送企业微信通知** | 使用企业微信机器人 MCP 发送群通知| ✅ |
| 9️⃣ | **打开 MR** | 提取并在 IDE 中内置浏览器进行预览 MR 地址，打开 MR 创建页面 | ✅ |

### 详细说明

#### 1️⃣ Git 变更分析
读取 `git-analyzer` skill 的内容并按照其指令执行 Git 命令分析整个仓库的变更。

**注意**：直接读取并执行 skill 文件内容，而不是调用 `use_skill` 工具。

#### 2️⃣ 构建验证
```bash
npm run build
```
❌ 构建失败则停止流程并报告错误。

#### 3️⃣ 版本号管理
调用 `version-manager` skill：
- 读取 `.changeset/` 目录中的所有 changeset 文件
- 根据变更类型确定新版本号（major/minor/patch）
- 更新 `package.json` 中的 `version`

#### 4️⃣ 发版分支管理
调用 `release-branch-manager` skill：
- 从 `package.json` 读取新版本号
- 创建或切换到发版分支（格式：`publish/tdesign-miniprogram-v{版本号}`）

#### 5️⃣ 合并 Changesets 并生成 Changelog
调用 `release-changelog-updater` skill：
- 读取并合并所有 changeset 文件
- 在 CHANGELOG.md 中插入新版本条目（头部）
- 删除已处理的 changeset 文件

#### 6️⃣ Release Notes 生成
调用 `release-notes-generator` skill：
- 从 CHANGELOG.md 提取最新版本内容
- 转换为用户友好的 Release Notes
- 保存到 `docs/release-notes/v{版本号}.md`
- 更新 Release Notes 索引

#### 7️⃣ 提交推送
**步骤**：
1. 调用 `commit-generator` skill 生成发版提交信息（type: `release(agent-cli)`）
2. 执行 Git 操作（单条 Bash 命令）：

```bash
REPO_ROOT=$(git rev-parse --show-toplevel) && \
git -C "$REPO_ROOT" add . && \
git -C "$REPO_ROOT" commit -m "$(cat <<'EOF'
<生成的提交信息>
EOF
)" && \
git -C "$REPO_ROOT" push -u origin $(git -C "$REPO_ROOT" branch --show-current)
```

**说明**：
- `git add .` 暂存整个仓库的变更（CHANGELOG.md、package.json、Release Notes、已删除的 changeset 文件）
- heredoc 内容不应有前导空格

#### 8️⃣ 企业微信通知
**步骤**：
1. 调用企业微信机器人 MCP 工具发送发版通知
2. 使用 `send_wework_message` 工具发送 Markdown 消息
3. 要求 MR 链接可以在企业微信中进行直接点击打开

**消息内容**：
```markdown
📦 **CodeBuddy Code 发版通知**

版本：v{版本号}
分支：{发版分支名称}
MR 链接：{MR链接}  

请 <@yeonzhou(周妍)> 进行 MR 合流主干审核 <@anlyyao(姚艳)>协助处理发版验收及后续运营事项准备。




yeonzhou(周妍)

**本次变更**：
{从 CHANGELOG 提取的主要变更内容摘要}
```

**注意**：
- 消息类型使用 `markdown`
- 需要 @提及相关负责人
- 包含版本号、分支名、MR 链接等关键信息

#### 9️⃣ 打开 MR 创建页面
从 `git push` 输出提取 MR 链接并使用 `open` 命令打开：

```bash
open "https://github.com/Tencent/tdesign-miniprogram/pull/4093"
```

---

## 🎯 关键规则

### ✅ 必须执行
- 构建验证（步骤 2）
- 版本号管理（步骤 3，更新 package.json）
- 合并 Changesets（步骤 5，使用 release-changelog-updater skill）
- Release Notes 生成（步骤 6，保存到 docs/release-notes/）
- 发版分支创建（步骤 4，如需要）
- 暂存所有文件（步骤 7，包括已删除的 changeset 文件）
- 企业微信通知（步骤 8，@汪晟杰、@孔德远）

### ❌ 严格禁止
- 使用 `gh` CLI 或 GitLab CLI
- 手动修改版本号（必须通过 version-manager skill）
- 跳过 changesets 合并步骤（必须使用 release-changelog-updater skill）
- 直接编辑 CHANGELOG.md 添加变更内容（应通过 changeset 文件管理）
- 使用 `yarn changeset:merge` 或 `changeset version` 命令（格式不符）
- 直接在 main 分支上提交发版变更
- 使用 `AskUserQuestion` 工具（应自动决策）

### 📦 Monorepo 特殊处理
- Git 操作在仓库根目录执行（`git rev-parse --show-toplevel`）
- 提交包含所有变更的包，不限于 `agent-cli`
- 版本号仅更新 `agent-cli` 的 `package.json`
- 发版分支命名包含 `codebuddy-code` 标识
- Changeset 文件位于 `packages/agent-cli/.changeset/`
- 已删除的 changeset 文件会被 `git add .` 暂存

### 📝 语义化版本规则
| 变更类型 | 版本递增 | 说明 |
|---------|---------|------|
| BREAKING CHANGE | Major +1 | 破坏性变更 |
| feat | Minor +1 | 新功能 |
| fix/docs/chore | Patch +1 | Bug 修复和改进 |

### 额外补充说明
$1  !!IMPORTANT

---

**开始智能发版流程...**
