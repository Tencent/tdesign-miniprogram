<view class="demo">
  <view class="demo-title">DropdownMenu 下拉菜单</view>
  <view class="demo-desc">菜单呈现数个并列的选项类目，用于整个页面的内容筛选，由菜单面板和菜单选项组成。</view>
  <t-demo title="01 类型" desc="单选下拉菜单">
    <t-dropdown-menu>
      <t-dropdown-item title="菜单名称" options="{{optionsS}}" value="{{selectedS}}" bindchange="singleSelected" />
      <t-dropdown-item
        title="菜单名称"
        options="{{optionsM}}"
        value="{{selectedM}}"
        bindchange="multiSelected"
        multiple
      />
    </t-dropdown-menu>
  </t-demo>

  <t-demo desc="多选下拉菜单">
    <t-dropdown-menu>
      <t-dropdown-item
        title="菜单名称"
        optionsColumns="2"
        options="{{optionsM}}"
        value="{{selectedM}}"
        bindchange="multiSelected"
        multiple
      />
      <t-dropdown-item
        title="菜单名称"
        optionsColumns="3"
        options="{{optionsM}}"
        value="{{selectedM}}"
        bindchange="multiSelected"
        multiple
      />
    </t-dropdown-menu>
  </t-demo>

  <t-demo desc="树形下拉菜单">
    <t-dropdown-menu>
      <t-dropdown-item
        title="菜单名称"
        optionsLayout="tree"
        options="{{treeOption}}"
        value="{{treeValue1}}"
        bindchange="tree1Selected"
      />
      <t-dropdown-item
        title="菜单名称"
        optionsLayout="tree"
        options="{{treeOption}}"
        value="{{treeValue2}}"
        bindchange="tree2Selected"
        multiple
      />
    </t-dropdown-menu>
  </t-demo>

  <t-demo title="02 状态" desc="下拉菜单状态"> </t-demo>
</view>
