<view class="demo">
  <view class="demo-title">Collapse 折叠面板</view>
  <view class="demo-desc">用于对复杂区域进行分组和隐藏 常用于订单信息展示等</view>

  <t-demo title="01 类型" desc="基础折叠面板">
    <!-- <t-collapse value="{{ active1 }}" data-key="active1" bind:change="changeBaseDemo" bind:open="open" bind:close="close">
      <t-collapse-panel
        title="折叠面板标题"
        labelWidth="{{item.labelWidth}}"
        content="{{item.content}}"
        name="{{item.name}}"
        icon="{{currentActive === 'active1' && item.expand ? 'arrow-up': 'arrow-down'}}"
      >
      </t-collapse-panel>
    </t-collapse> -->
  </t-demo>
  <t-collapse value="{{ active1 }}" data-key="active1" bind:change="changeBaseDemo" bind:open="open" bind:close="close">
    <t-collapse-panel
      wx:for="{{panels}}"
      wx:key-index="index"
      wx:key="index"
      title="{{item.title}}"
      extra="{{item.extra}}"
      labelWidth="{{item.labelWidth}}"
      content="{{item.content}}"
      name="{{item.name}}"
      icon="{{currentActive === 'active1' && item.expand ? 'arrow-up': 'arrow-down'}}"
    >
    </t-collapse-panel>
  </t-collapse>

  <t-collapse
    value="{{ active2 }}"
    accordion
    data-key="active2"
    bind:change="changeBaseDemo"
    bind:open="open"
    bind:close="close"
  >
    <t-collapse-panel
      wx:for="{{panels}}"
      wx:key-index="index"
      wx:key="index"
      title="{{item.title}}"
      extra="{{item.extra}}"
      labelWidth="{{item.labelWidth}}"
      content="{{item.content}}"
      name="{{item.name}}"
      icon="{{currentActive === 'active2' && item.expand ? 'arrow-up': 'arrow-down'}}"
    >
    </t-collapse-panel>
  </t-collapse>
</view>
