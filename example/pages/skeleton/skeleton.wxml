<view class="demo">
  <view class="demo-title">Skeleton 骨架屏</view>
  <view class="demo-desc"
    >用于等待加载内容所展示的占位图形组合，有动态效果加载效果，减少用户等待焦虑</view
  >
  <t-demo title="01 类型">
    <view class="demo-section__desc">基础</view>

    <t-skeleton
      t-class="skeleton-basic"
      row="4"
      rowWidth="{{rowWidth}}"
      rowHeight="{{rowHeight}}"
    ></t-skeleton>

    <view class="demo-section__desc">头像组合</view>
    <t-skeleton
      t-class="skeleton-avatar"
      t-class-avatar="skeleton-avatar-size"
      row="4"
      rowWidth="{{rowWidth2}}"
      rowHeight="{{rowHeight2}}"
      theme="avatar-text"
    ></t-skeleton>

    <view class="demo-section__desc">图片组合</view>
    <view class="container-flex">
      <t-skeleton
        row="4"
        rowWidth="{{rowWidth3}}"
        rowHeight="{{rowHeight3}}"
        t-class-text="skeleton-text"
      ></t-skeleton>
      <t-skeleton
        row="4"
        rowWidth="{{rowWidth3}}"
        rowHeight="{{rowHeight3}}"
        t-class-text="skeleton-text"
      ></t-skeleton>
    </view>
  </t-demo>
  <t-demo title="02 动画效果">
    <view class="demo-section__desc">渐变加载动画</view>
    <t-skeleton
      t-class="skeleton-basic"
      row="4"
      rowWidth="{{rowWidth}}"
      rowHeight="{{rowHeight}}"
      animation="flashed"
    ></t-skeleton>
    <view class="demo-section__desc">闪烁加载动画</view>
    <t-skeleton
      t-class="skeleton-avatar"
      t-class-avatar="skeleton-avatar-size"
      row="4"
      rowWidth="{{rowWidth2}}"
      rowHeight="{{rowHeight2}}"
      theme="avatar-text"
      animation="flashed"
    ></t-skeleton>
  </t-demo>
</view>
