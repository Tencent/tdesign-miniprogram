<view class="tdesign-mobile-demo">
  <view class="title">Form 表单</view>
  <view class="summary">用于开启一个闭环的操作任务，如"删除"对象、"购买"商品等。</view>

  <view class="demo-block">
    <view class="demo-block-title">01 基础类型</view>
    <view class="demo-block-summary">基础表单</view>

    <view class="option">
      <view class="button-group">
        <t-button theme="{{isActive1 ? 'light' : 'default'}}" shape="round" data-name="isActive1" bind:tap="onClick">
          水平排布
        </t-button>
        <t-button theme="{{isActive2 ? 'light' : 'default'}}" shape="round" data-name="isActive2" bind:tap="onClick">
          竖直排布
        </t-button>
      </view>
      <t-cell title="禁用态">
        <t-switch slot="note" value="{{switchValue}}" bind:change="onChangeSwitch" />
      </t-cell>
    </view>
  </view>

  <view class="demo-block" wx:if="{{isActive1}}">
    <horizontal disabled="{{switchValue}}" />
  </view>

  <view class="demo-block" wx:if="{{isActive2}}">
    <vertical disabled="{{switchValue}}" />
  </view>
</view>
