/* eslint-disable */

/**
 * 该文件为脚本自动生成文件，请勿随意修改。如需修改请联系 PMC
 * */

export interface TdIconProps {
  /**
   * 图标颜色
   * @default ''
   */
  color?: string;
  /**
   * 图标名称或图片链接
   * @default ''
   */
  name: string;
  /**
   * 自定义图标前缀
   * @default ''
   */
  prefix?: string;
  /**
   * 图标大小, 如 `20`, `20px`, `48rpx`, 默认单位是 `px`
   * @default ''
   */
  size?: string | number;
  /**
   * 点击图标时触发
   */
  onClick?: () => void;
}
