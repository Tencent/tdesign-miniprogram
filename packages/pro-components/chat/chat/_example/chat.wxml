<t-navbar class="demo-navbar" title="Chat" leftArrow />
<view class="demo">
  <t-tabs defaultValue="{{0}}" space-evenly="{{false}}">
    <t-tab-panel label="基础使用" value="{{0}}">
      <base />
    </t-tab-panel>
    <t-tab-panel label="组合式用法" value="{{1}}">
      <hook-component />
    </t-tab-panel>
    <t-tab-panel label="自定义" value="{{2}}">
      <custom />
    </t-tab-panel>
    <!-- 本次迭代先不上 -->
    <!-- <t-tab-panel label="代码助手" value="{{3}}">
      <codedemo />
    </t-tab-panel> -->
    <t-tab-panel label="文案助手" value="{{4}}">
      <docs />
    </t-tab-panel>
    <t-tab-panel label="图像生成" value="{{5}}">
      <imagedemo />
    </t-tab-panel>
    <t-tab-panel label="任务规划" value="{{6}}">
      <agent />
    </t-tab-panel>
  </t-tabs>
</view>